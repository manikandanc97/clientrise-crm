// dashboard types

export interface StatCardType {
  title: string;
  value: string;
  change: string;
  positive: boolean;
}

export interface ActivityType {
  id: number;
  title: string;
  time: string;
}
